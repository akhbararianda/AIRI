DM-Native.iss b/installer/AIRI-DM-Native.iss
index 5724dc8..d5d3d9d 100644
--- a/installer/AIRI-DM-Native.iss
+++ b/installer/AIRI-DM-Native.iss
@@ -1,5 +1,5 @@
 #define MyAppName "AIRI Download Manager"
-#define MyAppVersion "2.0.0-dev1"
+#define MyAppVersion "2.1.0"
 #define MyAppExeName "AIRI Download Manager.exe"
 
 [Setup]
@@ -8,7 +8,7 @@ AppName={#MyAppName}
 AppVersion={#MyAppVersion}
 DefaultDirName={localappdata}\Programs\AIRI Download Manager
 DefaultGroupName=AIRI Download Manager
-OutputBaseFilename=AIRI-Download-Manager-Native-v2.0.0-dev1-Setup
+OutputBaseFilename=AIRI-Download-Manager-Setup-v2
 OutputDir=Output
 Compression=lzma2
 SolidCompression=yes
@@ -26,6 +26,10 @@ Source: "..\build-win\Release\AIRI Download Manager.exe"; DestDir: "{app}"; Flag
 Source: "..\build-win\Release\AIRIDMBridge.exe"; DestDir: "{app}"; Flags: ignoreversion
 Source: "..\browser-extension\*"; DestDir: "{app}\browser-extension"; Flags: ignoreversion recursesubdirs createallsubdirs
 Source: "..\assets\airi.ico"; DestDir: "{app}"; Flags: ignoreversion
+Source: "..\third_party\tools\yt-dlp.exe"; DestDir: "{app}\tools"; Flags: ignoreversion
+Source: "..\third_party\tools\deno.exe"; DestDir: "{app}\tools"; Flags: ignoreversion
+Source: "..\third_party\tools\ffmpeg.exe"; DestDir: "{app}\tools"; Flags: ignoreversion
+Source: "..\third_party\tools\ffprobe.exe"; DestDir: "{app}\tools"; Flags: ignoreversion
 
 [Icons]
 Name: "{group}\AIRI Download Manager"; Filename: "{app}\{#MyAppExeName}"
diff --git a/src/app/app.rc b/src/app/app.rc
index 25cd67f..254a5f6 100644
--- a/src/app/app.rc
+++ b/src/app/app.rc
@@ -1,15 +1,16 @@
+#pragma code_page(65001)
 #include <windows.h>
 #include "resource.h"
 IDI_AIRI ICON "../../assets/airi.ico"
 IDD_ADD_URL DIALOGEX 0, 0, 360, 155
 STYLE DS_SETFONT | DS_MODALFRAME | WS_POPUP | WS_CAPTION | WS_SYSMENU
-CAPTION "Add download — AIRI Download Manager"
+CAPTION "Add download - AIRI Download Manager"
 FONT 9, "Segoe UI"
 BEGIN
     LTEXT "URL", -1, 12, 12, 330, 10
-    EDITTEXT IDC_URL, 12, 25, 336, 22, ES_AUTOHSCROLL
-    LTEXT "Filename (optional — AIRI accepts any file type)", -1, 12, 55, 250, 10
-    EDITTEXT IDC_FILENAME, 12, 68, 336, 22, ES_AUTOHSCROLL
+    EDITTEXT IDC_URL, 12, 25, 336, 22, ES_AUTOHSCROLL | WS_TABSTOP
+    LTEXT "Filename (optional - AIRI accepts any file type)", -1, 12, 55, 250, 10
+    EDITTEXT IDC_FILENAME, 12, 68, 336, 22, ES_AUTOHSCROLL | WS_TABSTOP
     LTEXT "Connections", -1, 12, 99, 70, 10
     COMBOBOX IDC_CONNECTIONS, 82, 96, 65, 80, CBS_DROPDOWNLIST | WS_VSCROLL | WS_TABSTOP
     DEFPUSHBUTTON "Start download", IDOK, 218, 122, 130, 22
diff --git a/src/app/main.cpp b/src/app/main.cpp
index bd72242..564834d 100644
--- a/src/app/main.cpp
+++ b/src/app/main.cpp
@@ -18,6 +18,8 @@
 #include <optional>
 #include <thread>
 #include <iterator>
+#include <mutex>
+#include <atomic>
 
 #pragma comment(lib, "comctl32.lib")
 
@@ -26,35 +28,80 @@ constexpr wchar_t kWindowClass[] = L"AIRI.DownloadManager.MainWindow";
 constexpr UINT WM_AIRI_REFRESH = WM_APP + 1;
 constexpr UINT WM_AIRI_MEDIA = WM_APP + 2;
 constexpr int ID_ADD = 3001, ID_PAUSE = 3002, ID_RESUME = 3003, ID_CANCEL = 3004, ID_OPEN = 3005;
+constexpr int ID_LIST = 3100, ID_SIDEBAR = 3101;
+constexpr std::uint64_t kMediaIdBase = 0x4000000000000000ULL;
 
 HWND g_main{}, g_sidebar{}, g_list{}, g_status{}, g_header{};
+HWND g_btn_add{}, g_btn_pause{}, g_btn_resume{}, g_btn_cancel{}, g_btn_open{};
 std::unique_ptr<airi::DownloadManager> g_manager;
 HFONT g_font{}, g_title_font{};
 HBRUSH g_bg{}, g_sidebar_brush{};
+std::wstring g_notice = L"Ready";
+std::mutex g_notice_mutex;
+std::atomic<std::uint64_t> g_next_media_id{kMediaIdBase};
+
+struct MediaUiJob {
+    std::uint64_t id{};
+    std::wstring title;
+    std::wstring status = L"Queued";
+    std::wstring detail;
+    int progress{};
+    std::filesystem::path directory;
+    bool finished{};
+    bool failed{};
+};
+std::mutex g_media_mutex;
+std::vector<MediaUiJob> g_media_jobs;
 
 std::wstring u8w(const std::string& s) {
     if (s.empty()) return {};
     int n = MultiByteToWideChar(CP_UTF8, 0, s.data(), static_cast<int>(s.size()), nullptr, 0);
-    std::wstring out(n, L'\0'); MultiByteToWideChar(CP_UTF8, 0, s.data(), static_cast<int>(s.size()), out.data(), n); return out;
+    std::wstring out(n, L'\0');
+    MultiByteToWideChar(CP_UTF8, 0, s.data(), static_cast<int>(s.size()), out.data(), n);
+    return out;
 }
 std::string w8u(const std::wstring& s) {
     if (s.empty()) return {};
     int n = WideCharToMultiByte(CP_UTF8, 0, s.data(), static_cast<int>(s.size()), nullptr, 0, nullptr, nullptr);
-    std::string out(n, '\0'); WideCharToMultiByte(CP_UTF8, 0, s.data(), static_cast<int>(s.size()), out.data(), n, nullptr, nullptr); return out;
+    std::string out(n, '\0');
+    WideCharToMultiByte(CP_UTF8, 0, s.data(), static_cast<int>(s.size()), out.data(), n, nullptr, nullptr);
+    return out;
+}
+
+void set_notice(std::wstring text) {
+    {
+        std::scoped_lock lock(g_notice_mutex);
+        g_notice = std::move(text);
+    }
+    if (g_main) PostMessageW(g_main, WM_AIRI_REFRESH, 0, 0);
 }
 
 void register_native_bridge() {
-    std::wstring exe(32768, L'\0'); DWORD n=GetModuleFileNameW(nullptr, exe.data(), static_cast<DWORD>(exe.size())); exe.resize(n);
-    auto dir=std::filesystem::path(exe).parent_path();
-    auto bridge=dir/L"AIRIDMBridge.exe"; if(!std::filesystem::exists(bridge)) return;
-    auto manifest=dir/L"com.airi.downloadmanager.json";
-    std::string bridgeUtf=w8u(bridge.wstring()); std::string escaped;
-    escaped.reserve(bridgeUtf.size()+16); for(char c:bridgeUtf){if(c=='\\')escaped+="\\\\";else if(c=='\"')escaped+="\\\"";else escaped+=c;}
-    std::ofstream f(manifest,std::ios::binary|std::ios::trunc);
+    std::wstring exe(32768, L'\0');
+    DWORD n = GetModuleFileNameW(nullptr, exe.data(), static_cast<DWORD>(exe.size()));
+    exe.resize(n);
+    auto dir = std::filesystem::path(exe).parent_path();
+    auto bridge = dir / L"AIRIDMBridge.exe";
+    if (!std::filesystem::exists(bridge)) return;
+    auto manifest = dir / L"com.airi.downloadmanager.json";
+    std::string bridgeUtf = w8u(bridge.wstring()), escaped;
+    escaped.reserve(bridgeUtf.size() + 16);
+    for (char c : bridgeUtf) { if (c == '\\') escaped += "\\\\"; else if (c == '"') escaped += "\\\""; else escaped += c; }
+    std::ofstream f(manifest, std::ios::binary | std::ios::trunc);
     f << "{\n  \"name\": \"com.airi.downloadmanager\",\n  \"description\": \"AIRI Download Manager Native C++ Bridge\",\n  \"path\": \"" << escaped << "\",\n  \"type\": \"stdio\",\n  \"allowed_origins\": [\"chrome-extension://knmjnpnbngjeilejfdhccehndiengjan/\"]\n}\n";
     f.close();
-    const wchar_t* keys[]={L"Software\\Google\\Chrome\\NativeMessagingHosts\\com.airi.downloadmanager",L"Software\\Microsoft\\Edge\\NativeMessagingHosts\\com.airi.downloadmanager"};
-    for(auto* key:keys){HKEY h{};if(RegCreateKeyExW(HKEY_CURRENT_USER,key,0,null