diff --git a/CMakeLists.txt b/CMakeLists.txt
index bde28ca..a3b6a7a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,9 +1,12 @@
 cmake_minimum_required(VERSION 3.24)
-project(AIRIDownloadManagerNative VERSION 2.0.0 LANGUAGES CXX)
+project(AIRIDownloadManagerNative VERSION 2.1.0 LANGUAGES CXX)
 
 set(CMAKE_CXX_STANDARD 20)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
 set(CMAKE_CXX_EXTENSIONS OFF)
+if(MSVC)
+    add_compile_options(/utf-8 /W4 /permissive-)
+endif()
 
 add_library(airi_common STATIC
     src/common/airi_json.cpp
@@ -12,6 +15,11 @@ add_library(airi_common STATIC
 )
 target_include_directories(airi_common PUBLIC src)
 
+enable_testing()
+add_executable(airi_core_tests tests/core_tests.cpp)
+target_link_libraries(airi_core_tests PRIVATE airi_common)
+add_test(NAME airi_core_tests COMMAND airi_core_tests)
+
 if(WIN32)
     add_library(airi_engine STATIC
         src/engine/download_engine.cpp
@@ -32,9 +40,4 @@ if(WIN32)
     add_executable(AIRIDMBridge WIN32 src/bridge/main.cpp)
     target_compile_definitions(AIRIDMBridge PRIVATE UNICODE _UNICODE WIN32_LEAN_AND_MEAN NOMINMAX)
     target_link_libraries(AIRIDMBridge PRIVATE airi_common shell32 shlwapi)
-else()
-    enable_testing()
-    add_executable(airi_core_tests tests/core_tests.cpp)
-    target_link_libraries(airi_core_tests PRIVATE airi_common)
-    add_test(NAME airi_core_tests COMMAND airi_core_tests)
 endif()
diff --git a/browser-extension/manifest.json b/browser-extension/manifest.json
index 0107756..06bd5cc 100644
--- a/browser-extension/manifest.json
+++ b/browser-extension/manifest.json
@@ -1,22 +1,52 @@
 {
   "manifest_version": 3,
   "name": "AIRI Download Manager Integration",
-  "version": "3.0.0",
-  "description": "Native Windows integration for AIRI Download Manager. Captures browser downloads regardless of file extension.",
-  "permissions": ["downloads", "nativeMessaging", "storage", "contextMenus", "cookies"],
-  "host_permissions": ["http://*/*", "https://*/*"],
-  "background": {"service_worker": "service-worker.js"},
-  "content_scripts": [{
-    "matches": ["https://www.youtube.com/*", "https://youtube.com/*"],
-    "js": ["youtube-overlay.js"],
-    "css": ["youtube-overlay.css"],
-    "run_at": "document_idle"
-  }],
+  "version": "3.1.0",
+  "description": "Native Windows integration for AIRI Download Manager v2.1. Captures all HTTP/HTTPS downloads and sends page-media jobs to the desktop engine.",
+  "permissions": [
+    "downloads",
+    "nativeMessaging",
+    "storage",
+    "contextMenus",
+    "cookies"
+  ],
+  "host_permissions": [
+    "http://*/*",
+    "https://*/*"
+  ],
+  "background": {
+    "service_worker": "service-worker.js"
+  },
+  "content_scripts": [
+    {
+      "matches": [
+        "https://www.youtube.com/*",
+        "https://youtube.com/*"
+      ],
+      "js": [
+        "youtube-overlay.js"
+      ],
+      "css": [
+        "youtube-overlay.css"
+      ],
+      "run_at": "document_idle"
+    }
+  ],
   "action": {
     "default_title": "AIRI Download Manager",
     "default_popup": "popup.html",
-    "default_icon": {"16":"icons/icon16.png","32":"icons/icon32.png","48":"icons/icon48.png","128":"icons/icon128.png"}
+    "default_icon": {
+      "16": "icons/icon16.png",
+      "32": "icons/icon32.png",
+      "48": "icons/icon48.png",
+      "128": "icons/icon128.png"
+    }
+  },
+  "icons": {
+    "16": "icons/icon16.png",
+    "32": "icons/icon32.png",
+    "48": "icons/icon48.png",
+    "128": "icons/icon128.png"
   },
-  "icons": {"16":"icons/icon16.png","32":"icons/icon32.png","48":"icons/icon48.png","128":"icons/icon128.png"},
   "key": ""
 }
diff --git a/browser-extension/service-worker.js b/browser-extension/service-worker.js
index bcc4e9c..e563148 100644
--- a/browser-extension/service-worker.js
+++ b/browser-extension/service-worker.js
@@ -65,13 +65,17 @@ chrome.contextMenus.onClicked.addListener(async (info, tab) => {
     await nativeSend({ action: 'download', url: info.linkUrl, filename: '', referer: tab?.url || '', cookie: await cookieHeader(info.linkUrl), userAgent: navigator.userAgent || '', source: 'context-menu' });
   }
   if (info.menuItemId === 'airi-download-page' && tab?.url) {
-    await nativeSend({ action: 'media_page', pageUrl: tab.url, title: tab.title || 'Media', quality: 'best', source: 'context-menu' });
+    await nativeSend({ action: 'media_page', pageUrl: tab.url, title: tab.title || 'Media', quality: 'best', cookie: await cookieHeader(tab.url), referer: tab.url, userAgent: navigator.userAgent || '', source: 'context-menu' });
   }
 });
 
 chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
   if (message?.type === 'airi-media-page') {
-    nativeSend({ action: 'media_page', pageUrl: message.pageUrl || sender.tab?.url || '', title: message.title || sender.tab?.title || 'Media', quality: message.quality || 'best', source: 'youtube-overlay' }).then(sendResponse);
+    (async () => {
+      const pageUrl = message.pageUrl || sender.tab?.url || '';
+      const result = await nativeSend({ action: 'media_page', pageUrl, title: message.title || sender.tab?.title || 'Media', quality: message.quality || 'best', cookie: await cookieHeader(pageUrl), referer: pageUrl, userAgent: navigator.userAgent || '', source: 'youtube-overlay' });
+      sendResponse(result);
+    })();
     return true;
   }
   if (message?.type === 'airi-bridge-ping') {
diff --git a/browser-extension/youtube-overlay.js b/browser-extension/youtube-overlay.js
index 84d5d75..92e58ee 100644
--- a/browser-extension/youtube-overlay.js
+++ b/browser-extension/youtube-overlay.js
@@ -21,8 +21,13 @@
       e.stopPropagation();
       const old = b.textContent; b.textContent = 'Sending to AIRI…'; b.disabled = true;
       const res = await chrome.runtime.sendMessage({ type:'airi-media-page', pageUrl:location.href, title:document.title.replace(/\s*-\s*YouTube\s*$/i,''), quality:b.dataset.q });
-      b.textContent = res?.ok ? '✓ Sent to AIRI Desktop' : 'Desktop bridge unavailable';
-      setTimeout(() => { b.textContent=old; b.disabled=false; menu.classList.remove('show'); }, 1800);
+      if (res?.ok) {
+        b.textContent = '✓ Started in AIRI';
+      } else {
+        const why = res?.error || res?.response?.message || 'Desktop rejected request';
+        b.textContent = 'Failed: ' + why.slice(0, 42);
+      }
+      setTimeout(() => { b.textContent=old; b.disabled=false; if(res?.ok) menu.classList.remove('show'); }, 2600);
     });
     root.append(button, menu); player.appendChild(root); lastUrl = location.href;
   }
diff --git a/installer/AIRI-